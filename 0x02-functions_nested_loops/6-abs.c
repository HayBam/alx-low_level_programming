#include "main.h"

/**
 * _abs - To check the absolute number of integers
 *
 * @n: number to check for
 *
 * Return: Always return 1 if int is positive
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
