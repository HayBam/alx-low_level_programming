#include <ctype.h>

/**
 * main - This function checks for uppercase character
 *
 * Returns: Always return zero if successful
 */
int _isupper(int c)
{
	isupper(c);

	if (c == 1)
		return (1);
	else
		return (0);
}
