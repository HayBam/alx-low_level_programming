#include <ctype.h>

/**
 * _isdigit - This function detects digits
 *
 * @c: This is the data type for the argument
 *
 * Return: Always return zero if successful
 */
int _isdigit(int c)
{
	if (isdigit(c)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
